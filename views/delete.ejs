<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles.css">
    <title><%= item.name %></title>
</head>
<body>
    <main class="edit-main">
        <h1>Delete Item</h1>
        <form action="" method="post" class="create-item-form">
            <input type="hidden" name="delete-id" value="<%= item.id %>" readonly>
            <label for="deleteName">Name</label>
            <input type="text" name="deleteName" placeholder="Name" value="<%= item.name %>">
            <label for="deleteCategorie">Categorie</label>
            <input type="text" name="deleteCategorie" placeholder="Categorie" value="<%= item.categorie %>" readonly>
            <label for="deleteSize">Size</label>
            <input type="text" name="deleteSize" placeholder="Size (XS,S,M,L,XL,XXL)" value="<%= item.size %>" readonly>
            <label for="deleteColor">Color</label>
            <input type="text" name="deleteColor" placeholder="Color:" value="<%= item.color %>" readonly>
            <label for="deletePrice">Price</label>
            <input type="number" name="deletePrice" placeholder="Price" value="<%= item.price %>" readonly>
            <label for="password">Password:</label>
            <input type="password" id="password" name="password" required>
            <button type="submit" id="edit-item-submit">Delete product</button>
        </form>
    </main>

    <%- include('footer') %>
</body>
</html>